.cache/
.claude/
.agents/
build*/
.DS_Store
writeup-overleaf/
docs/presentation-build/
docs/latex-build/
# LaTeX build artifacts (LaTeX Workshop / latexmk -outdir) in any latex-build/ subfolder
**/latex-build/
.ai-bridge/
__pycache__/
.venv/
.vscode/
util/data/
visualization/
scenarios/data/*.dat
outputs/
/tmp/Chromosphere-CPP/
# scratch run directory used by scripts/release_validation.sh
/tmp/release_validation/

# Generated simulation outputs and per-run logs (if written to root).
out_*.txt
out_*.log
output.log
output.txt
output_*.txt
output_*.log
*.out
*.mp4

# Generated Doxygen output. docs/doxygen/html/ is DELIBERATELY TRACKED so the
# reference is browsable straight from the repository (and servable by GitHub
# Pages) without anyone having to install doxygen. Rebuild it with
# scripts/build_docs.sh before committing — the pre-commit hook in .githooks/
# does that automatically once you run scripts/install_hooks.sh.
docs/doxygen/doxygen-warnings.log
