# Figures for the 2026-08-17 group meeting

Kept inside the talk's own folder so the deck stays buildable from a fresh clone.
`visualization/` is gitignored, so a deck that reached across into it would not build for anyone else.

| File | Provenance |
| --- | --- |
| `c7_ionization_profile_compare.png` | Copy of `visualization/reference/c7_ionization_profile_compare.png`. Generated by `util/plot_c7_ionization_profile.py --mode compare` (catalogued in `visualize_commands.md`). Note the script writes to `visualization/eos_gamma/`; the `reference/` copy was placed by hand. Its CRASH curve needs `outputs/eos_gamma/gamma_hydrogen_excitation.dat`, which is **not** in the tree — regenerate with `bash util/eos/build_and_run.sh excitation` before re-running the script. |
| `precision_ftotal_4000s.png` | Crop of panel (b) of `visualization/model_column/precision_control_N500_4000s.png` |
| `precision_ulp_mechanism.png` | Crop of panel (f). **Not used** — the precision result is one aside slide, and the ULP mechanism is stated in a sentence instead. Kept for the "how do you know?" question. |
| `precision_mass_budget.png` | Crop of panel (e). **Not used** — unreadable at slide size. |
| `precision_base_zoom.png` | Crop of panel (c). **Not used** — kept as a backup asset for the first-face-artifact question. |

Only `c7_ionization_profile_compare.png` and `precision_ftotal_4000s.png` appear in the deck; the other three are backup assets. The four crops are reproduced by the snippet recorded in `visualize_commands.md` section 15. The parent figure comes from `util/plot_precision_control.py`; see `docs/studies/numerics/float32_precision_control_experiment.md`.
